{
  "name": "@surveyqs/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, Zod schemas and the API client shared between web/tenant-app and mobile.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no shared tests yet)\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "jwt-decode": "^4.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
