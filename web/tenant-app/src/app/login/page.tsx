/**
 * The App Router requires `page.tsx` as the route entry, so this file is a
 * one-line re-export. The real code is in LoginPage.tsx.
 */
export const dynamic = "force-dynamic";
export { default } from "./LoginPage";
