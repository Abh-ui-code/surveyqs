DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

DATABASE_URL=postgres://surveyqs:surveyqs@localhost:5432/surveyqs_dev
REDIS_URL=redis://localhost:6379/0

TENANT_BASE_DOMAIN=surveyqs.local
TENANT_PLATFORM_SUBDOMAINS=admin,api,www,app

# Leave blank to keep the corresponding integration inert (console email,
# in-app-only notifications, no error reporting). See
# docs/operations/ENVIRONMENT.md for the full reference.
EMAIL_PROVIDER_API_KEY=
SENTRY_DSN=
DEMO_ADMIN_EMAIL=
DEMO_ADMIN_PASSWORD=
